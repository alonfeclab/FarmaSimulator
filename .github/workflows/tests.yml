# Compila y corre los tests unitarios (motor de cálculo puro en src/simcore.cpp)
# y los tests gráficos QML (Qt Quick Test, en tests/qml/).
name: Tests

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: jurplel/install-qt-action@v4
        with:
          version: '6.11.1'

      - name: Configurar y compilar
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
          cmake --build build --parallel --target farmaciasim_tests farmaciasim_qml_tests

      - name: Ejecutar tests
        env:
          QT_QPA_PLATFORM: offscreen
        run: ctest --test-dir build --output-on-failure
